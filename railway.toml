[build]
builder = "nixpacks"
buildCommand = "npm ci && npm run build"

[deploy]
startCommand = "node_modules/.bin/serve dist --single --listen tcp://0.0.0.0:${PORT:-3000}"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3